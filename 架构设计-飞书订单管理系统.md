# 系统架构设计文档
## 飞书订单管理系统

**版本**: v1.0  
**日期**: 2026-05-10  
**架构师**: 高见远  
**基于 PRD**: G:\WorkBuddy file\2026-05-10-task-1\PRD-飞书订单管理系统.md

---

## 一、实现方案 🏗️

### 1.1 方案选择：飞书多维表格 + 自定义侧边栏

**推荐方案**：✅ **飞书多维表格 + 自定义 HTML/JS 侧边栏**

**理由**：
1. ✅ **零部署成本**：无需独立服务器，直接嵌入飞书
2. ✅ **数据一致性**：数据直接存在飞书多维表格，无需同步
3. ✅ **权限复用**：自动继承飞书的权限管理
4. ✅ **快速迭代**：前端修改即时生效
5. ✅ **移动端支持**：飞书移动端自动适配

**技术架构图**：
```
┌─────────────────────────────────────────────────────┐
│                   飞书客户端                          │
│  ┌───────────────────────────────────────────────┐  │
│  │         飞书多维表格（数据存储）               │  │
│  │  - 订单表（主表）                            │  │
│  │  - 配置表（汇率等）                          │  │
│  └───────────────────────────────────────────────┘  │
│                        ↕                            │
│  ┌───────────────────────────────────────────────┐  │
│  │    自定义侧边栏（HTML + JavaScript）          │  │
│  │  - 订单列表                                  │  │
│  │  - 订单编辑/新建                             │  │
│  │  - 利润计算（前端实时计算）                   │  │
│  │  - 飞书多维表格 API 调用                     │  │
│  └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
            ↕ (可选) 飞书开放平台 API
┌─────────────────────────────────────────────────────┐
│         飞书开放平台（OAuth 2.0）                  │
│  - 读取/写入多维表格数据                           │
│  - 上传附件（图片）                               │
└─────────────────────────────────────────────────────┘
```

---

## 二、技术栈选型 🛠️

### 2.1 前端（侧边栏）
| 技术 | 选型 | 理由 |
|------|------|------|
| **框架** | **原生 HTML + CSS + JavaScript** | 飞书侧边栏环境，无需构建工具 |
| **CSS 框架** | **Tailwind CSS (CDN)** | 快速开发，响应式设计 |
| **HTTP 库** | **Axios (CDN)** | 调用飞书 API |
| **UI 组件** | **飞书 UI 组件库** | 与飞书风格一致 |
| **表格** | **Tabulator** | 轻量级，功能强大 |

### 2.2 后端（可选，仅用于复杂计算）
| 技术 | 选型 | 理由 |
|------|------|------|
| **运行时** | **无后端（纯前端）** | 利润计算可在前端完成 |
| **备选** | Node.js + Express | 如果前端计算性能不足 |

### 2.3 数据存储
| 技术 | 选型 | 理由 |
|------|------|------|
| **主存储** | **飞书多维表格** | 直接存储订单数据 |
| **配置** | **飞书多维表格（配置表）** | 存储汇率等配置 |

### 2.4 API
| 技术 | 选型 | 理由 |
|------|------|------|
| **API** | **飞书多维表格 API** | 官方 SDK，稳定可靠 |
| **认证** | **OAuth 2.0 (飞书开放平台)** | 已配置（app_id: cli_aa8a30778e385bb5） |

---

## 三、文件列表及相对路径 📁

```
feishu-order-system/
├── README.md                           # 项目说明
├── PRD-飞书订单管理系统.md             # 产品需求文档
├── 架构设计-飞书订单管理系统.md        # 本文档
├── src/                                # 源代码目录
│   ├── sidebar.html                    # 主入口（侧边栏）
│   ├── css/
│   │   └── style.css                  # 自定义样式
│   ├── js/
│   │   ├── app.js                     # 主应用逻辑
│   │   ├── api.js                     # 飞书 API 封装
│   │   ├── calculator.js              # 利润计算器（核心）
│   │   ├── components/
│   │   │   ├── OrderList.js          # 订单列表组件
│   │   │   ├── OrderForm.js         # 订单表单组件
│   │   │   ├── OrderDetail.js        # 订单详情组件
│   │   │   └── ConfigPanel.js        # 配置面板组件
│   │   └── utils/
│   │       ├── validator.js          # 表单验证
│   │       └── formatter.js          # 数据格式化
│   └── assets/
│       └── icons/                     # 图标资源
├── test/                              # 测试目录
│   ├── calculator.test.js             # 利润计算单元测试
│   └── api.test.js                   # API 调用测试
└── docs/                              # 文档目录
    ├── API参考.md                     # 飞书 API 使用说明
    └── 部署指南.md                   # 如何部署到飞书
```

---

## 四、数据结构和接口（类图）📊

### 4.1 核心数据结构

#### Order（订单）
```javascript
class Order {
  // 基础信息
  date: Date                  // 日期
  remark: string              // 备注（取消、测试单等）
  orderNo: string             // 单号（唯一）
  
  // 商品信息
  image: File | null         // 图片（附件）
  productNo: string          // 货号
  productName: string        // 名称
  quantity: number           // 数量
  
  // 采购信息
  platform: '拼多多' | '1688' | '淘宝' | '其他'  // 采购平台
  purchaseNote: string       // 采购备注
  purchaseLink: string       // 采购链接（URL）
  
  // 财务信息
  cost: number               // 成本（¥）
  unitPrice: number          // 单价（¥）
  unitPriceRUB: number       // 单价（卢布）（自动计算）
  commission: number         // 佣金（¥）
  shippingFee: number        // 运费（¥）（自动计算）
  weight: number             // 重量（kg）
  shippingMethod: '空运' | '陆空联运' | '陆运'  // 运输方式
  lastMile: number           // 尾程（¥）（自动计算）
  tax: number                // 税（¥）（自动计算）
  profit: number             // 利润（¥）（自动计算）
  
  // 状态
  status: '待处理' | '采购中' | '已发货' | '已完成' | '取消'
}
```

#### Config（配置）
```javascript
class Config {
  rubleExchangeRate: number   // 卢布汇率（¥对₽）
  updatedAt: Date             // 最后更新时间
}
```

### 4.2 飞书多维表格字段映射

| Order 字段 | 飞书字段名 | 飞书字段类型 |
|------------|------------|--------------|
| date | 日期 | 日期 |
| remark | 备注 | 文本 |
| orderNo | 单号 | 文本（单行） |
| image | 图片 | 附件 |
| productNo | 货号 | 文本（单行） |
| productName | 名称 | 文本（单行） |
| quantity | 数量 | 数字 |
| platform | 采购平台 | 单选 |
| purchaseNote | 采购备注 | 文本 |
| purchaseLink | 采购链接 | 文本（URL） |
| cost | 成本 | 货币（¥） |
| unitPrice | 单价 | 货币（¥） |
| unitPriceRUB | 单价（卢布） | 货币（₽） |
| commission | 佣金 | 货币（¥） |
| shippingFee | 运费 | 货币（¥） |
| weight | 重量 | 数字 |
| shippingMethod | 运输方式 | 单选 |
| lastMile | 尾程 | 货币（¥） |
| tax | 税 | 货币（¥） |
| profit | 利润 | 货币（¥） |
| status | 状态 | 单选 |

### 4.3 核心类图

```
┌─────────────────────────────────────────────────┐
│                  FeishuAPI                     │
├─────────────────────────────────────────────────┤
│ - appId: string                                │
│ - appSecret: string                            │
│ - baseToken: string                            │
│ - tableId: string                              │
├─────────────────────────────────────────────────┤
│ + constructor(config: Config)                  │
│ + getRecords(filter?: string): Promise<Order[]> │
│ + createRecord(order: Order): Promise<string>  │
│ + updateRecord(recordId: string, order: Order) │
│ + deleteRecord(recordId: string): Promise<void> │
│ + uploadImage(file: File): Promise<string>     │
└─────────────────────────────────────────────────┘
                        ↓ 使用
┌─────────────────────────────────────────────────┐
│               ProfitCalculator                  │
├─────────────────────────────────────────────────┤
│ - exchangeRate: number                         │
├─────────────────────────────────────────────────┤
│ + constructor(exchangeRate: number)            │
│ + calculateProfit(order: Partial<Order>): Order │
│ + calculateTax(unitPrice: number): number      │
│ + calculateLastMile(unitPriceRUB: number): number │
│ + calculateShippingFee(method, weight, unitPriceRUB): number │
│ + calculateUnitPriceRUB(unitPrice, exchangeRate): number │
└─────────────────────────────────────────────────┘
                        ↓ 返回
┌─────────────────────────────────────────────────┐
│                  OrderApp                       │
├─────────────────────────────────────────────────┤
│ - api: FeishuAPI                               │
│ - calculator: ProfitCalculator                  │
│ - currentOrder: Order | null                   │
├─────────────────────────────────────────────────┤
│ + init(): void                                 │
│ + loadOrders(): Promise<void>                  │
│ + createOrder(data: Partial<Order>): Promise<void> │
│ + updateOrder(id: string, data): Promise<void> │
│ + deleteOrder(id: string): Promise<void>       │
│ + onFieldChange(field: string, value: any): void │
└─────────────────────────────────────────────────┘
```

---

## 五、程序调用流程（时序图）🔄

### 5.1 订单创建流程

```
用户           OrderApp        ProfitCalculator      FeishuAPI      飞书服务器
 │                │                    │                   │                │
 │── 填写表单 ──→│                    │                   │                │
 │                │                    │                   │                │
 │── 输入单价 ──→│                    │                   │                │
 │                │── calculateProfit()─→│                   │                │
 │                │                    │── 计算单价（卢布） │                │
 │                │                    │── 计算税           │                │
 │                │                    │── 计算尾程         │                │
 │                │                    │── 计算运费         │                │
 │                │                    │── 计算利润         │                │
 │                │←── 返回完整订单数据 ──│                   │                │
 │                │                    │                   │                │
 │── 点击保存 ──→│                    │                   │                │
 │                │── createRecord()──→│                   │                │
 │                │                    │── POST /bitable/v1/apps/{app_token}/tables/{table_id}/records │
 │                │                    │                   ├──→│                │
 │                │                    │                   │   │── 写入多维表格 │
 │                │                    │                   │←─┤                │
 │                │                    │←── 返回 record_id ─┤                │
 │                │←── 成功 ──────────│                   │                │
 │←── 显示成功 ──│                    │                   │                │
```

### 5.2 订单列表加载流程

```
用户           OrderApp        FeishuAPI         飞书服务器
 │                │                │                  │
 │── 打开侧边栏 ─→│                │                  │
 │                │── loadOrders()→│                  │
 │                │                │── GET /bitable/v1/apps/{app_token}/tables/{table_id}/records?page_size=100 │
 │                │                ├─────────────────→│
 │                │                │                  │── 查询多维表格
 │                │                │←── 返回记录列表 ──┤
 │                │                │                  │
 │                │←── Order[] ────│                  │
 │                │                │                  │
 │←── 渲染表格 ──│                │                  │
```

### 5.3 实时计算流程（用户输入时）

```
用户           OrderForm       ProfitCalculator
 │                │                  │
 │── 修改单价 ──→│                  │
 │                │── onFieldChange('unitPrice', 100) │
 │                │                  │
 │                │── calculateProfit(this.order) ──→│
 │                │                  │
 │                │                  │── tax = 100 * 0.03 = 3
 │                │                  │── unitPriceRUB = 100 * 89.12 = 8912
 │                │                  │── lastMile = MAX(MIN(8912*0.02,200),15)/11.89 = 15
 │                │                  │── shippingFee = 根据运输方式/重量/单价计算
 │                │                  │── profit = 100 - 成本 - 佣金 - 运费 - 3 - 15
 │                │                  │
 │                │←── 返回更新后的订单 ──│
 │                │                  │
 │                │── 更新表单显示 ──→│
 │                │                  │
 │←── 看到实时计算结果 ──│                  │
```

---

## 六、任务列表（有序、含依赖关系）📋

### Phase 1: 基础设施（Day 1-2）

| 任务 ID | 任务名称 | 依赖 | 预计时间 | 说明 |
|---------|----------|------|----------|------|
| T1.1 | 创建飞书多维表格 | 无 | 0.5h | 创建订单表和配置表，定义字段 |
| T1.2 | 配置飞书开放平台应用 | 无 | 1h | 启用多维表格读写权限，获取 app_id/app_secret |
| T1.3 | 创建侧边栏 HTML 骨架 | 无 | 1h | 基础 HTML 结构，引入 CSS/JS CDN |
| T1.4 | 封装飞书 API 调用（api.js） | T1.2 | 2h | 封装 CRUD 操作，处理认证 |
| T1.5 | 实现 ProfitCalculator（calculator.js） | 无 | 3h | **核心**：实现所有利润计算算法 |

### Phase 2: 核心功能（Day 3-5）

| 任务 ID | 任务名称 | 依赖 | 预计时间 | 说明 |
|---------|----------|------|----------|------|
| T2.1 | 实现订单列表组件（OrderList.js） | T1.3, T1.4 | 2h | 表格展示，分页，搜索 |
| T2.2 | 实现订单表单组件（OrderForm.js） | T1.3, T1.5 | 3h | 新建/编辑表单，表单验证 |
| T2.3 | 实现实时计算逻辑 | T1.5, T2.2 | 2h | 单价/重量/运输方式变更时实时重新计算 |
| T2.4 | 实现订单详情组件（OrderDetail.js） | T2.1 | 1.5h | 只读展示，利润明细 |
| T2.5 | 实现配置面板（ConfigPanel.js） | T1.3, T1.4 | 1.5h | 汇率设置 |

### Phase 3: 集成与测试（Day 6-7）

| 任务 ID | 任务名称 | 依赖 | 预计时间 | 说明 |
|---------|----------|------|----------|------|
| T3.1 | 集成所有组件（app.js） | T2.1 - T2.5 | 2h | 主应用逻辑，事件绑定 |
| T3.2 | 编写单元测试（calculator.test.js） | T1.5 | 2h | 测试所有利润计算场景 |
| T3.3 | 手动测试完整流程 | T3.1 | 2h | 新建→计算→保存→查看 |
| T3.4 | 修复 Bug | T3.3 | 2h | 根据测试结果修复 |

### Phase 4: 部署与文档（Day 8-9）

| 任务 ID | 任务名称 | 依赖 | 预计时间 | 说明 |
|---------|----------|------|----------|------|
| T4.1 | 部署侧边栏到飞书多维表格 | T3.4 | 1h | 上传 HTML，配置侧边栏 |
| T4.2 | 编写用户使用文档 | T4.1 | 2h | 操作手册，常见问题 |
| T4.3 | 编写部署文档（docs/部署指南.md） | T4.1 | 1h | 如何部署到飞书 |
| T4.4 | 用户培训 | T4.2 | 1h | 培训用户使用 |

---

## 七、依赖包列表 📦

### 7.1 前端 CDN 引入（无需 npm install）

```html
<!-- CSS -->
<link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
<link href="https://unpkg.com/tabulator-tables@5.4.4/dist/css/tabulator.min.css" rel="stylesheet">

<!-- JavaScript -->
<script src="https://unpkg.com/axios@1.6.0/dist/axios.min.js"></script>
<script src="https://unpkg.com/tabulator-tables@5.4.4/dist/js/tabulator.min.js"></script>
<script src="https://unpkg.com/luxon@3.3.0/build/global/luxon.min.js"></script>
```

### 7.2 开发依赖（可选，用于本地测试）

```json
{
  "devDependencies": {
    "live-server": "^1.2.2",     // 本地 HTTP 服务器
    "jest": "^29.7.0"             // 单元测试
  }
}
```

### 7.3 飞书 SDK

- **无需安装**：直接调用飞书 REST API（已通过 lark-cli 测试）

---

## 八、共享知识（跨文件约定）📚

### 8.1 字段命名约定

| 用途 | 命名规则 | 示例 |
|------|----------|------|
| 飞书字段名 | 中文（与 PRD 一致） | `单价（卢布）` |
| JavaScript 变量 | camelCase | `unitPriceRUB` |
| 函数名 | camelCase，动词开头 | `calculateProfit()` |
| 常量 | UPPER_SNAKE_CASE | `MAX_LAST_MILE_FEE = 200` |
| CSS 类名 | kebab-case（Tailwind 风格） | `order-form` |

### 8.2 利润计算精度

- **货币字段**：保留两位小数（¥/₽）
- **重量字段**：保留三位小数（kg）
- **汇率字段**：保留四位小数

### 8.3 错误处理约定

```javascript
// API 调用错误处理
try {
  const records = await api.getRecords();
} catch (error) {
  console.error('[FeishuAPI] 获取记录失败:', error);
  showNotification('error', '加载失败，请稍后重试');
}

// 计算错误处理
try {
  const profit = calculator.calculateProfit(order);
} catch (error) {
  console.error('[ProfitCalculator] 计算失败:', error);
  return { ...order, profit: 0, _error: error.message };
}
```

### 8.4 飞书 API 限流

- **限流规则**：每秒最多 10 次请求
- **应对策略**：批量操作时使用 `batch` API，或添加延迟

```javascript
// 批量创建（推荐）
await api.batchCreateRecords(orders);

// 单条创建（需限速）
for (const order of orders) {
  await api.createRecord(order);
  await sleep(100); // 延迟 100ms
}
```

### 8.5 汇率存储

- **配置表结构**：
  ```
  配置表（单条记录）
  - ruble_exchange_rate: 89.12 (数字)
  - updated_at: 2026-05-10 21:55:39 (日期时间)
  ```

- **读取汇率**：
  ```javascript
  const config = await api.getConfig();
  const exchangeRate = config.rubleExchangeRate;
  ```

---

## 九、核心算法实现（ProfitCalculator）💻

### 9.1 计算税

```javascript
/**
 * 计算税
 * @param {number} unitPrice - 单价（¥）
 * @returns {number} 税（¥，保留两位小数）
 */
calculateTax(unitPrice) {
  return Math.round(unitPrice * 0.03 * 100) / 100;
}
```

### 9.2 计算尾程

```javascript
/**
 * 计算尾程
 * @param {number} unitPriceRUB - 单价（卢布）
 * @returns {number} 尾程（¥，保留两位小数）
 */
calculateLastMile(unitPriceRUB) {
  const feeRUB = Math.max(Math.min(unitPriceRUB * 0.02, 200), 15);
  return Math.round(feeRUB / 11.89 * 100) / 100;
}
```

### 9.3 计算运费（核心复杂逻辑）

```javascript
/**
 * 计算运费
 * @param {string} method - 运输方式（空运/陆空联运/陆运）
 * @param {number} weight - 重量（kg）
 * @param {number} unitPriceRUB - 单价（卢布）
 * @returns {number} 运费（¥，保留两位小数）
 */
calculateShippingFee(method, weight, unitPriceRUB) {
  // 定义运费规则：[重量下限, 重量上限, 单价下限, 单价上限, 系数, 常数]
  const rules = [
    // 单价（卢布）1-1500
    ['空运',     0.001, 0.5,  1, 1500, 46.8,  3.12],
    ['陆空联运', 0.001, 0.5,  1, 1500, 36.4,  3.12],
    ['陆运',     0.001, 0.5,  1, 1500, 26.0,  3.00],
    ['陆空联运', 0.5,   30,   1, 1500, 26.0, 23.92],
    ['陆运',     0.5,   30,   1, 1500, 17.68, 23.92],
    
    // 单价（卢布）1500-7000
    ['空运',     0.001, 2,    1500, 7000, 46.8, 16.64],
    ['陆空联运', 0.001, 2,    1500, 7000, 36.4, 16.64],
    ['陆运',     0.001, 2,    1500, 7000, 26.0, 16.64],
    ['陆空联运', 2,     30,   1500, 7000, 26.0, 37.44],
    ['陆运',     2,     30,   1500, 7000, 17.68, 37.44],
    
    // 单价（卢布）7000-250000
    ['空运',     0.001, 5,    7000, 250000, 46.8, 22.88],
    ['陆空联运', 0.001, 5,    7000, 250000, 36.4, 22.88],
    ['陆运',     0.001, 5,    7000, 250000, 26.0, 22.88],
    ['陆空联运', 5,     30,   7000, 250000, 29.12, 64.48],
    ['陆运',     5,     30,   7000, 250000, 23.92, 64.48],
  ];
  
  // 查找匹配的规则
  for (const [ruleMethod, minW, maxW, minP, maxP, factor, constant] of rules) {
    if (
      method === ruleMethod &&
      weight >= minW &&
      weight <= maxW &&
      unitPriceRUB >= minP &&
      unitPriceRUB <= maxP
    ) {
      const fee = factor * weight + constant;
      return Math.round(fee * 100) / 100;
    }
  }
  
  // 无匹配规则，返回 0
  return 0;
}
```

### 9.4 完整利润计算

```javascript
/**
 * 计算完整订单利润
 * @param {Partial<Order>} order - 订单数据（部分字段）
 * @returns {Order} 完整的订单数据（含所有计算结果）
 */
calculateProfit(order) {
  const {
    unitPrice = 0,      // 单价（¥）
    cost = 0,            // 成本（¥）
    commission = 0,      // 佣金（¥）
    weight = 0,          // 重量（kg）
    shippingMethod = '陆运',  // 运输方式
  } = order;
  
  // 计算中间值
  const unitPriceRUB = Math.round(unitPrice * this.exchangeRate * 100) / 100;
  const tax = this.calculateTax(unitPrice);
  const lastMile = this.calculateLastMile(unitPriceRUB);
  const shippingFee = this.calculateShippingFee(shippingMethod, weight, unitPriceRUB);
  
  // 计算利润
  const profit = unitPrice - cost - commission - shippingFee - tax - lastMile;
  
  return {
    ...order,
    unitPriceRUB,
    tax,
    lastMile,
    shippingFee,
    profit: Math.round(profit * 100) / 100,
  };
}
```

---

## 十、待明确事项 ❓

### 10.1 飞书应用配置

- [ ] **侧边栏 URL**：侧边栏 HTML 文件应该托管在哪里？
  - 选项 A：上传到飞书文档，获取公开 URL
  - 选项 B：托管在自己的服务器（需要公网 IP）
  - 选项 C：本地开发时使用 `live-server`，部署时切换到选项 A

- [ ] **OAuth 回调地址**：飞书开放平台应用的回调地址应该填什么？

### 10.2 图片上传

- [ ] **图片存储方式**：
  - 选项 A：上传到飞书多维表格附件字段（推荐）
  - 选项 B：上传到飞书云空间（需要额外权限）

### 10.3 批量导入

- [ ] **Excel 导入格式**：用户提供的 Excel 文件应该是什么列顺序？
- [ ] **字段映射**：Excel 列名 ↔ 飞书字段名映射表

### 10.4 汇率更新

- [ ] **汇率来源**：
  - 选项 A：手动输入（简单）
  - 选项 B：自动爬取（需要定时任务）

### 10.5 权限管理

- [ ] **是否需要区分角色**？
  - 管理员：可编辑汇率，可删除订单
  - 财务：可查看利润，不可编辑
  - 运营：可新建/编辑订单

---

## 十一、风险评估 ⚠️

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 飞书 API 限流 | 批量操作失败 | 使用批量 API，添加延迟 |
| 计算精度问题 | 利润计算错误 | 使用 `Math.round()` 保留两位小数 |
| 浏览器兼容性 | 侧边栏无法正常显示 | 使用 CDN 引入的库，避免高级语法 |
| 网络异常 | API 调用失败 | 添加重试机制（最多 3 次） |

---

## 十二、后续迭代方向 🚀

### V2.0（P1 功能）
- [ ] 批量导入（Excel）
- [ ] 订单搜索/筛选（按日期、单号、货号等）
- [ ] 订单状态流转（待处理 → 采购中 → 已发货 → 已完成）
- [ ] 数据导出（Excel/CSV）

### V3.0（P2 功能）
- [ ] 数据报表（利润分析图表）
- [ ] 权限管理（角色区分）
- [ ] 消息通知（订单状态变更时发送飞书消息）
- [ ] 采购链接跳转（点击跳转到 1688/拼多多）

---

## 十三、附录 📎

### 13.1 飞书多维表格 API 参考

- **获取记录列表**：`GET /bitable/v1/apps/{app_token}/tables/{table_id}/records`
- **创建记录**：`POST /bitable/v1/apps/{app_token}/tables/{table_id}/records`
- **更新记录**：`PUT /bitable/v1/apps/{app_token}/tables/{table_id}/records/{record_id}`
- **删除记录**：`DELETE /bitable/v1/apps/{app_token}/tables/{table_id}/records/{record_id}`
- **批量创建**：`POST /bitable/v1/apps/{app_token}/tables/{table_id}/records/batch_create`
- **上传附件**：`POST /drive/v1/files/upload_all`

### 13.2 相关文档

- PRD 文档：`G:\WorkBuddy file\2026-05-10-task-1\PRD-飞书订单管理系统.md`
- 飞书开放平台文档：https://open.feishu.cn/document/server-docs/docs/bitable-v1/bitable-overview
- Tabulator 文档：https://tabulator.info/docs
- Tailwind CSS 文档：https://tailwindcss.com/docs

---

**文档状态**：✅ 待评审  
**下一步**：分发给工程师进行代码实现  
**预计开发时间**：2-3 周（按每天 6 小时计算）
