# 飞书侧边栏上传指南

## 上传步骤

### 1. 打包 dist 目录

在命令行执行：
```bash
cd "G:\WorkBuddy file\2026-05-10-task-1\dist"
tar -a -c -f order-system-sidebar.zip *
```

或者右键 `dist` 文件夹 → 发送到 → 压缩(zipped)文件夹，改名为 `order-system-sidebar.zip`

---

### 2. 登录飞书开放平台

访问：https://open.feishu.cn/

1. 登录账号
2. 进入"开发者后台"
3. 找到应用：**订单管理系统**（App ID: `cli_aa8a2b386b8dbed`）

---

### 3. 上传侧边栏

1. 左侧菜单找到 **"应用侧边栏"**
2. 点击 **"多维表格侧边栏"**
3. 点击 **"创建侧边栏"**
4. 填写信息：
   - **侧边栏名称**：`订单管理系统`
   - **上传 ZIP**：选择 `order-system-sidebar.zip`
   - **入口文件**：`sidebar.html`
5. 点击 **"保存"**

---

### 4. 设置可见范围

1. 在侧边栏管理页面，找到 **"可见范围"**
2. 选择 **"所有成员"**（或指定部门/成员）
3. 保存

---

### 5. 发布应用

1. 左侧菜单 → **"版本管理与发布"**
2. 点击 **"创建版本"**
3. 填写：
   - **版本号**：`1.0.0`
   - **更新说明**：`初始版本，支持订单管理、利润自动计算`
4. 点击 **"提交审核"**
5. 审核通过后点击 **"发布"**

---

## 验证部署

发布后：

1. 打开多维表格：https://jcnqhz19oa35.feishu.cn/base/Xg16bELkqa2u68s2qsvcay2LnJf
2. 点击右侧 **"侧边栏"** 图标
3. 选择 **"订单管理系统"**
4. 测试功能是否正常

---

## ⚠️ 重要提醒

**config.js 含有敏感信息，绝对不要上传！**

已打包的文件（✅ 安全）：
- `sidebar.html`
- `js/api.js`
- `js/app.js`
- `js/calculator.js`
- `js/components/*.js`
- `js/utils/*.js`
- `js/config.template.js`

**不要打包的文件（❌ 敏感）：
- `js/config.js` ← 已排除
- `js/node_modules/` ← 已排除
