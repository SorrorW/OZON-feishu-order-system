# 部署检查清单 ✅

在将应用部署到飞书多维表格侧边栏之前，请完成以下所有检查项。

---

## 第一阶段：飞书应用配置

### 1. 创建飞书应用
- [ ] 访问 [飞书开放平台](https://open.feishu.cn/)
- [ ] 登录并进入"开发者后台"
- [ ] 点击"创建企业自建应用"
- [ ] 填写应用名称：`订单管理系统`
- [ ] 上传应用图标（可选）
- [ ] 记录 `App ID` 和 `App Secret`

### 2. 配置应用权限
在"权限管理"中开启以下权限：

**多维表格权限：**
- [ ] `bitable:app:readonly`（查看多维表格）
- [ ] `bitable:app:readwrite`（读写多维表格）

**云文档权限：**
- [ ] `drive:drive:readonly`（查看云文档）

**获取权限方法：**
1. 进入"权限管理"
2. 搜索上述权限名称
3. 点击"开通权限"
4. 如需审核，提交审核申请

### 3. 配置应用版本
- [ ] 上传应用图标（推荐尺寸 96x96）
- [ ] 填写应用简介
- [ ] 填写应用描述
- [ ] 提交版本审核（如需上架应用商店）

### 4. 获取应用凭证
- [ ] 进入"凭证与基础信息"
- [ ] 复制 `App ID`（例如：`cli_aa8a30778e385bb5`）
- [ ] 复制 `App Secret`（例如：`p6xYPzPOZbOwYwg8QjXPIhQAc4RYNSqB`）
- [ ] **⚠️ 妥善保管 App Secret，不要提交到 Git**

---

## 第二阶段：多维表格配置

### 1. 创建多维表格
- [ ] 在飞书中创建新的多维表格
- [ ] 命名为：`订单管理数据表`
- [ ] 记录 `base_token`（URL 中的字符串，例如：`D2RybBryyaB27QsYp7pcj5xGnfA`）

**获取 base_token 方法：**
- 打开多维表格
- 查看浏览器地址栏：`https://xxx.feishu.cn/base/XXXXXXXXX`
- `XXXXXXXXX` 即为 `base_token`

### 2. 创建订单表
在多维表格中创建"订单表"，包含以下字段：

| 字段名称 | 字段类型 | 说明 |
|---------|---------|------|
| 日期 | 日期 | 订单日期 |
| 单号 | 文本 | 唯一订单号 |
| 货号 | 文本 | 商品货号 |
| 名称 | 文本 | 商品名称 |
| 数量 | 数字 | 购买数量 |
| 成本 | 货币 | 采购成本 |
| 单价 | 货币 | 销售单价（人民币） |
| 单价（卢布） | 货币 | 销售单价（卢布） |
| 佣金 | 货币 | 平台佣金 |
| 运费 | 货币 | 国际运费 |
| 重量 | 数字 | 商品重量（kg） |
| 运输方式 | 单选 | 空运/陆空联运/陆运 |
| 尾程 | 货币 | 尾程物流费用 |
| 税 | 货币 | 税费（单价×3%） |
| 利润 | 货币 | 最终利润 |
| 状态 | 单选 | 待处理/采购中/已发货/已完成/取消 |
| 图片 | 附件 | 商品图片 |
| 采购平台 | 文本 | 采购平台名称 |
| 采购链接 | 文本 | 采购商品链接 |
| 备注 | 文本 | 订单备注 |
| 采购备注 | 文本 | 采购备注 |

- [ ] 记录订单表的 `table_id`

**获取 table_id 方法：**
- 点击订单表标签
- 查看浏览器地址栏：`...table/XXXXXXXXXX`
- `XXXXXXXXXX` 即为 `table_id`

### 3. 创建配置表
在多维表格中创建"配置表"，包含以下字段：

| 字段名称 | 字段类型 | 说明 | 示例值 |
|---------|---------|------|--------|
| 卢布汇率 | 数字 | 人民币兑卢布汇率 | 89.12 |
| 更新时间 | 日期时间 | 最后更新时间 | 2026-05-10 22:00 |

- [ ] 在配置表中添加一行数据，设置默认汇率（例如：89.12）
- [ ] 记录配置表的 `table_id`

---

## 第三阶段：本地配置

### 1. 创建配置文件
- [ ] 复制配置模板：`cp src/js/config.template.js src/js/config.js`
- [ ] 编辑 `src/js/config.js`，填写真实配置：

```javascript
const FEISHU_CONFIG = {
  appId: 'cli_xxxxxxxxxxxx',           // 替换为真实的 App ID
  appSecret: 'xxxxxxxxxxxxxxxx',        // 替换为真实的 App Secret
  baseToken: 'D2RybBryyaB27QsYp7pcj5xGnfA',   // 替换为真实的 base_token
  tableId: 'tblxxxxxxxxxx',            // 替换为订单表的 table_id
  configTableId: 'tblxxxxxxxxxx',      // 替换为配置表的 table_id
  apiEndpoint: 'https://open.feishu.cn/open-apis'
};
```

### 2. 验证配置
- [ ] 检查 `config.js` 中的配置项是否填写完整
- [ ] 确认 `config.js` 已添加到 `.gitignore`（防止误提交）
- [ ] 运行本地测试服务器：
  ```bash
  cd src
  live-server --entry-file=sidebar.html
  ```
- [ ] 浏览器访问 `http://localhost:8080`
- [ ] 检查是否能成功连接到飞书 API
- [ ] 检查是否能读取订单数据和配置

### 3. 运行测试
- [ ] 运行单元测试，确保利润计算正确：
  ```bash
  cd test
  node calculator.test.js
  ```
- [ ] 预期输出：所有测试通过 ✅

---

## 第四阶段：部署到飞书

### 1. 准备部署文件
- [ ] 确保所有代码文件已保存
- [ ] 确认 `config.js` **不会**被上传（已在 .gitignore 中）
- [ ] 将 `src/` 目录下的所有文件打包（除了 `config.js`）

**需要部署的文件：**
```
sidebar.html
js/app.js
js/api.js
js/calculator.js
js/config.template.js    # 只上传模板，不上传真实配置
js/components/OrderList.js
js/components/OrderForm.js
js/components/OrderDetail.js
js/components/ConfigPanel.js
js/utils/formatter.js
js/utils/validator.js
```

**不要部署的文件：**
```
js/config.js              # 真实配置文件，含敏感信息
```

### 2. 上传到飞书多维表格
有两种方式：

#### 方式 A：使用飞书开放平台上传（推荐）
1. 进入飞书开放平台
2. 找到你的应用
3. 进入"应用侧边栏" → "多维表格侧边栏"
4. 点击"上传侧边栏"
5. 将 `sidebar.html` 及所有相关 JS/CSS 文件打包成 ZIP
6. 上传 ZIP 文件
7. 设置侧边栏名称和图标
8. 保存并发布

#### 方式 B：使用飞书多维表格直接上传
1. 打开多维表格
2. 点击右上角"..." → "侧边栏" → "添加侧边栏"
3. 上传 `sidebar.html`
4. 系统会自动处理依赖（CDN 引入的库无需上传）

### 3. 配置侧边栏权限
- [ ] 进入"应用管理" → "侧边栏设置"
- [ ] 设置可见范围（所有成员/指定部门/指定成员）
- [ ] 保存设置

### 4. 测试部署
- [ ] 打开多维表格
- [ ] 点击右侧"侧边栏"图标
- [ ] 选择"订单管理系统"
- [ ] 检查应用是否正常加载
- [ ] 测试订单创建功能
- [ ] 测试利润自动计算
- [ ] 测试订单列表展示
- [ ] 测试配置修改（汇率更新）

---

## 第五阶段：上线后验证

### 1. 功能验证
- [ ] 创建测试订单，验证利润计算是否正确
- [ ] 编辑订单，验证实时计算是否生效
- [ ] 删除订单，验证删除功能
- [ ] 测试搜索、筛选功能
- [ ] 测试数据导出（CSV）
- [ ] 修改汇率配置，验证是否生效

### 2. 性能验证
- [ ] 加载 100 条订单数据，检查加载速度
- [ ] 检查是否有报错（打开浏览器控制台）
- [ ] 检查 API 调用是否有限流问题

### 3. 安全验证
- [ ] 确认 `config.js` 未被提交到 Git
- [ ] 确认 App Secret 未暴露在前端代码中
- [ ] 确认 API 权限最小化（只开启必要的权限）

---

## 常见问题排查

### 问题 1：应用无法加载
**原因：** 配置文件缺失或配置错误
**解决：**
1. 检查 `config.js` 是否存在
2. 检查 `config.js` 中的配置项是否填写正确
3. 打开浏览器控制台，查看错误信息

### 问题 2：API 调用失败（401 错误）
**原因：** App ID 或 App Secret 错误
**解决：**
1. 检查 `config.js` 中的 `appId` 和 `appSecret` 是否正确
2. 确认飞书应用已启用
3. 确认应用权限已开启

### 问题 3：无法读取多维表格数据（404 错误）
**原因：** `baseToken`、`tableId` 错误
**解决：**
1. 检查 `baseToken` 是否正确（从 URL 获取）
2. 检查 `tableId` 是否正确（从 URL 获取）
3. 确认应用有访问该多维表格的权限

### 问题 4：利润计算不准确
**原因：** 汇率配置错误
**解决：**
1. 打开配置面板，检查汇率是否准确
2. 运行单元测试验证计算器：`node test/calculator.test.js`

---

## 部署完成 ✅

如果以上所有检查项都已完成，恭喜！你的应用已成功部署到飞书多维表格侧边栏。

**下一步：**
- 培训用户如何使用系统
- 收集用户反馈
- 规划 V2.0 功能（批量导入、高级筛选等）

---

**文档版本：** 1.0  
**最后更新：** 2026-05-10  
**维护者：** 齐活林（Qi）· 交付总监
